import Session from "../model/Session.js";
import User from "../model/User.js";

export const listSession= async(req,res,next)=>{
    try {
        const userId = req.usr.userId;
        const {status} = req.query;
        
    } catch (error) {
        
    }
}